import React from "react";
import { ScrollView } from "react-native";
import Home from "./Home";
function index() {
  return (
    <ScrollView>
      <Home />
    </ScrollView>
  );
}

export default index;
